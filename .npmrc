# 本仓库默认从官方 registry 安装，保证 package-lock.json 的 resolved 始终是
# registry.npmjs.org 形态（开源仓库通用；私有镜像地址一旦写进 lock，外部用户会装不上）。
# 内部 CI 用 --registry=<mirror> 命令行参数覆盖即可；本地想临时走镜像同理。
registry=https://registry.npmjs.org/
